# 🛡️ Analytical-Intelligence
> نظام SIEM احترافي مصغر مع كشف الهجمات في الوقت الحقيقي باستخدام Suricata + نماذج تعلم الآلة

---

## 📋 نظرة عامة

Mini-SIEM هو نظام كشف التهديدات الأمنية في الوقت الحقيقي يتكون من:

| المكون | الوظيفة | زمن الاستجابة |
|--------|---------|---------------|
| **Suricata IDS** | كشف الهجمات بالتوقيعات (أساسي) | ~1 ثانية |
| **SSH LSTM** | كشف محاولات الاختراق عبر SSH | 1-3 ثواني |
| **Network ML** | تصنيف حركة الشبكة (ثانوي) | 2-8 ثواني |

---

## 🏗️ هيكل النظام

```
┌─────────────────────────────────────────────────────────────────┐
│                    خادم الاستشعار (Sensor)                      │
│                      192.168.1.10                               │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌──────────┐  ┌──────────────────────────┐   │
│  │auth_collector│  │ suricata │  │    flow_collector        │   │
│  │(tail auth.log)│ │(AF_PACKET)│  │     (NFStream)           │   │
│  └──────┬──────┘  └────┬─────┘  └───────────┬──────────────┘   │
│         │              │                    │                   │
│         │        ┌─────┴──────┐             │                   │
│         │        │suricata_   │             │                   │
│         │        │shipper     │             │                   │
│         │        └─────┬──────┘             │                   │
└─────────┼──────────────┼───────────────────┼───────────────────┘
          │              │                   │
          │   POST /api/v1/ingest/* (X-API-Key)
          └──────────────┼───────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    خادم التحليل (Analysis)                      │
│                      192.168.1.20:8000                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Backend (FastAPI)                      │  │
│  │  ┌──────────┐   ┌──────────┐   ┌──────────────────┐      │  │
│  │  │SSH LSTM  │   │ Suricata │   │   Network ML     │      │  │
│  │  │Detector  │   │ Processor│   │   Classifier     │      │  │
│  │  └──────────┘   └──────────┘   └──────────────────┘      │  │
│  │                                                           │  │
│  │  ┌───────────────────────────────────────────────────┐   │  │
│  │  │          UI (Jinja2 + CSS + JavaScript)           │   │  │
│  │  │   Dashboard | Alerts | Events | Models            │   │  │
│  │  └───────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────┬───────────────────────────┘  │
│                                 ▼                               │
│               ┌──────────────────────────────┐                 │
│               │         PostgreSQL           │                 │
│               │   raw_events | detections    │                 │
│               └──────────────────────────────┘                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📡 خطة الشبكة والمنافذ

| الخادم | العنوان | المنفذ | الوصف |
|--------|---------|--------|-------|
| Analysis | 192.168.1.20 | 8000/tcp | واجهة الويب + API |
| Sensor | 192.168.1.10 | - | (لا منافذ واردة) |
| PostgreSQL | داخلي فقط | 5432 | **غير مكشوف للخارج** |

### 🔥 إعداد جدار الحماية (UFW)

```bash

# (مهم) قبل تفعيل UFW اسمح بالـ SSH حتى لا تنقطع عليك الإدارة
sudo ufw allow 22/tcp

# السماح للـ Sensor فقط يرسل للـ API/UI
sudo ufw allow from 192.168.1.10 to any port 8000 proto tcp

# السماح لجهازك (Windows) لفتح الداشبورد (عدّل IP حق جهازك)
sudo ufw allow from <YOUR_WINDOWS_IP> to any port 8000 proto tcp

# منع أي أحد غيرهم
sudo ufw deny 8000/tcp

# تفعيل + تحقق
sudo ufw enable
sudo ufw status numbered
```

---

## ⚡ التثبيت والتشغيل

### المتطلبات

- Docker 20.10+
- Docker Compose v2+
- نظام Linux (Ubuntu/Debian/Kali) للاستشعار
- واجهة الشبكة `ens33` (أو غيرها - قابل للتغيير)

---

### 📊 1. إعداد خادم التحليل (192.168.1.20)

```bash
# 1. تثبيت Docker
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker $USER

# 2. استنساخ المشروع
git clone https://github.com/Amr204/Analytical-Intelligence.git Analytical-Intelligence
cd Analytical-Intelligence

# 3. صلاحيات سكربتات التشغيل
chmod +x scripts/*.sh

# 4. إنشاء ملف .env (اختياري - لتغيير API Key)
cp .env.example .env
nano .env  # تعديل INGEST_API_KEY

# 5. تشغيل النظام (أفضل خيار: سكربت جاهز)
bash scripts/analysis_up.sh

# بديل (يدوي):
# docker compose -f docker-compose.analysis.yml up -d --build
# انتظار التشغيل (30 ثانية تقريباً)
# sleep 30

# 6. التحقق من الصحة
curl http://localhost:8000/api/v1/health

# 7. فتح الواجهة
# في المتصفح: http://192.168.1.20:8000
```

---

### 🔍 2. إعداد خادم الاستشعار (192.168.1.10)

```bash
# 1. تثبيت Docker
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker $USER

# (مهم) طبّق صلاحية مجموعة docker بدون تسجيل خروج
newgrp docker

# 2. استنساخ المشروع
git clone https://github.com/Amr204/Analytical-Intelligence.git Analytical-Intelligence
cd Analytical-Intelligence

# 3. صلاحيات سكربتات التشغيل
chmod +x scripts/*.sh

# 4. التحقق من اسم واجهة الشبكة
ip link show
# مثال: ens33, eth0, enp0s3

# 5. إعداد المتغيرات
export NET_IFACE=ens33                        # غيّر حسب واجهتك
export ANALYZER_URL=http://192.168.1.20:8000
export X_API_KEY=test-api-key-12345           # نفس المفتاح في Analysis

# 5. إنشاء ملف .env
cat > .env << EOF
NET_IFACE=${NET_IFACE}
ANALYZER_URL=${ANALYZER_URL}
X_API_KEY=${X_API_KEY}
DEVICE_ID=sensor-01
HOSTNAME=$(hostname)
DEVICE_IP=192.168.1.10
FLOW_IDLE_TIMEOUT=2
FLOW_ACTIVE_TIMEOUT=5
EOF

# 6. التحقق من الاتصال بخادم التحليل
ping -c 3 192.168.1.20
curl http://192.168.1.20:8000/api/v1/health

# 7. تشغيل النظام (أفضل خيار: سكربت جاهز)
bash scripts/sensor_up.sh

# بديل (يدوي):
# docker compose -f docker-compose.sensor.yml up -d --build

# 8. مراقبة السجلات
docker compose -f docker-compose.sensor.yml logs -f
```

---

## ✅ التحقق من التشغيل

### 1. فحص الـ Health

```bash
curl -s http://192.168.1.20:8000/api/v1/health | jq
# المتوقع: {"status":"ok","timestamp":"...","version":"1.0.0"}
```

### 2. إرسال أحداث تجريبية

```bash
cd Analytical-Intelligence

# إرسال 10 أحداث من كل نوع
python scripts/sample_sender.py \
  --url http://192.168.1.20:8000 \
  --api-key test-api-key-12345 \
  --count 10

# إرسال أحداث SSH فقط
python scripts/sample_sender.py --type auth --count 20

# إرسال أحداث Suricata فقط
python scripts/sample_sender.py --type suricata --count 20
```

### 3. مراقبة تنبيهات Suricata

```bash
# على خادم الاستشعار
tail -f data/suricata/eve.json | jq '.alert.signature // empty'
```

### 4. التحقق من قاعدة البيانات

```bash
# على خادم التحليل
docker exec -it minisiem-postgres psql -U siem -d minisiem

-- عدد الأحداث
SELECT event_type, COUNT(*) FROM raw_events GROUP BY event_type;

-- عدد التنبيهات
SELECT model_name, severity, COUNT(*) FROM detections GROUP BY model_name, severity;

-- آخر 10 تنبيهات
SELECT ts, model_name, label, severity FROM detections ORDER BY ts DESC LIMIT 10;

\q
```

---

## 🎬 سيناريو كامل: من الصفر إلى رؤية التنبيهات

> [!NOTE]
> **هذا السيناريو شامل خطوة بخطوة** - اتبعه بالترتيب من البداية للنهاية لتشغيل النظام ورؤية التنبيهات في لوحة التحكم.

### 📋 المتطلبات الأولية

قبل البدء، تأكد من توفر:

| المتطلب | التفاصيل |
|---------|----------|
| **خادمين** | Analysis (192.168.1.20) + Sensor (192.168.1.10) |
| **نظام التشغيل** | Ubuntu 22.04 LTS أو Kali Linux |
| **الاتصال** | الخادمين على نفس الشبكة ويمكنهما التواصل |
| **صلاحيات** | وصول root أو sudo |
| **الموارد** | RAM: 4GB+ لكل خادم، مساحة: 20GB+ |

---

### 🖥️ المرحلة 1: تجهيز خادم التحليل (Analysis Server)

> **الخادم:** 192.168.1.20
> **المدة المتوقعة:** 10-15 دقيقة

#### الخطوة 1.1: تعيين IP ثابت

```bash
# تحرير إعدادات الشبكة
sudo nano /etc/netplan/00-installer-config.yaml
```

أضف المحتوى التالي:

```yaml
network:
  version: 2
  ethernets:
    ens33:          # غيّر حسب واجهتك
      dhcp4: no
      addresses:
        - 192.168.1.20/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

```bash
# تطبيق الإعدادات
sudo netplan apply

# التحقق
ip addr show
# يجب أن ترى: 192.168.1.20
```

#### الخطوة 1.2: تثبيت Docker

```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Docker
curl -fsSL https://get.docker.com | sudo sh

# إضافة المستخدم لمجموعة Docker
sudo usermod -aG docker $USER

# تسجيل الخروج والدخول مرة أخرى (مهم!)
exit
# ثم سجل دخول مرة أخرى

# التحقق
docker --version
# المتوقع: Docker version 24.x.x أو أحدث

docker compose version
# المتوقع: Docker Compose version v2.x.x
```

#### الخطوة 1.3: تحميل المشروع

```bash
# الانتقال للمجلد الرئيسي
cd ~

# استنساخ المشروع
git clone https://github.com/Amr204/Analytical-Intelligence.git

# الدخول للمشروع
cd Analytical-Intelligence

# التحقق من الملفات
ls -la
# يجب أن ترى: docker-compose.analysis.yml, README.md, services/, agents/, etc.
```

#### الخطوة 1.4: التحقق من النماذج

```bash
# التحقق من وجود النماذج
ls -la models/ssh/
# المتوقع: ssh_lstm.joblib

ls -la models/network/
# المتوقع: model.joblib, feature_list.json, label_map.json, preprocess_config.json

# إذا كانت النماذج غير موجودة - انسخها من المجلدات الأصلية
# cp models/Host-Model/ssh_lstm.joblib models/ssh/
# cp models/Network-Model/* models/network/
```

#### الخطوة 1.5: إعداد ملف البيئة

```bash
# نسخ ملف البيئة النموذجي
cp .env.example .env

# تعديل الملف (اختياري - للتخصيص)
nano .env
```

**المحتوى المهم في .env:**
```bash
# مفتاح API - غيّره في الإنتاج!
INGEST_API_KEY=test-api-key-12345

# قاعدة البيانات
POSTGRES_USER=siem
POSTGRES_PASSWORD=siempass123
POSTGRES_DB=minisiem
```

#### الخطوة 1.6: تشغيل Analysis Stack

```bash
# بناء وتشغيل الحاويات
docker compose -f docker-compose.analysis.yml up -d --build

# مراقبة عملية البناء (أول مرة تأخذ 5-10 دقائق)
docker compose -f docker-compose.analysis.yml logs -f

# اضغط Ctrl+C للخروج من السجلات
```

#### الخطوة 1.7: التحقق من التشغيل

```bash
# انتظر 30 ثانية
sleep 30

# تحقق من حالة الحاويات
docker ps
# يجب أن ترى: minisiem-backend و minisiem-postgres بحالة "Up"

# فحص صحة الـ Backend
curl -s http://localhost:8000/api/v1/health | jq
```

**النتيجة المتوقعة:**
```json
{
  "status": "ok",
  "timestamp": "2026-01-12T17:30:00.000000Z",
  "version": "1.0.0"
}
```

#### الخطوة 1.8: فتح لوحة التحكم

```bash
# من جهازك الشخصي، افتح المتصفح وزر:
# http://192.168.1.20:8000

# أو من نفس الخادم:
curl http://localhost:8000 | head -20
```

**✅ المرحلة 1 مكتملة!** - لوحة التحكم تعمل، لكن بدون بيانات حتى الآن.

---

### 🔍 المرحلة 2: تجهيز خادم الاستشعار (Sensor Server)

> **الخادم:** 192.168.1.10
> **المدة المتوقعة:** 10-15 دقيقة

#### الخطوة 2.1: تعيين IP ثابت

```bash
sudo nano /etc/netplan/00-installer-config.yaml
```

```yaml
network:
  version: 2
  ethernets:
    ens33:
      dhcp4: no
      addresses:
        - 192.168.1.10/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
```

```bash
sudo netplan apply
ip addr show
```

#### الخطوة 2.2: تثبيت Docker

```bash
sudo apt update && sudo apt upgrade -y
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker $USER
exit
# سجل دخول مرة أخرى
```

#### الخطوة 2.3: التحقق من الاتصال بخادم التحليل

```bash
# اختبار الـ ping
ping -c 4 192.168.1.20
# يجب أن ترى ردود ناجحة

# اختبار الـ API
curl -s http://192.168.1.20:8000/api/v1/health
# يجب أن ترى: {"status":"ok"...}
```

> [!WARNING]
> **إذا فشل الاتصال:**
> - تأكد من جدار الحماية على خادم التحليل
> - تأكد أن الخادمين على نفس الشبكة
> - جرب: `sudo ufw allow 8000` على خادم التحليل

#### الخطوة 2.4: تحميل المشروع

```bash
cd ~
git clone https://github.com/Amr204/Analytical-Intelligence.git
cd Analytical-Intelligence
```

#### الخطوة 2.5: تحديد واجهة الشبكة

```bash
# عرض الواجهات المتاحة
ip link show

# ابحث عن الواجهة النشطة (عادة ens33 أو eth0 أو enp0s3)
# سجّل اسم الواجهة لاستخدامه في الخطوة التالية
```

**مثال الخرج:**
```
1: lo: <LOOPBACK,UP,LOWER_UP> ...
2: ens33: <BROADCAST,MULTICAST,UP,LOWER_UP> ...  ← هذه الواجهة
```

#### الخطوة 2.6: إنشاء ملف البيئة

```bash
# استبدل ens33 باسم واجهتك إذا كان مختلفاً
cat > .env << 'EOF'
# عنوان خادم التحليل
ANALYZER_URL=http://192.168.1.20:8000

# مفتاح API (يجب أن يتطابق مع خادم التحليل)
X_API_KEY=test-api-key-12345

# واجهة الشبكة للالتقاط
NET_IFACE=ens33

# معرف هذا الجهاز
DEVICE_ID=sensor-01
HOSTNAME=sensor-server
DEVICE_IP=192.168.1.10

# إعدادات تجميع التدفقات
FLOW_IDLE_TIMEOUT=2
FLOW_ACTIVE_TIMEOUT=5
EOF

# التحقق
cat .env
```

#### الخطوة 2.7: إعداد مجلد Suricata

```bash
# إنشاء مجلد السجلات
mkdir -p data/suricata

# التحقق
ls -la data/
```

#### الخطوة 2.8: تشغيل Sensor Stack

```bash
# بناء وتشغيل الحاويات
docker compose -f docker-compose.sensor.yml up -d --build

# البناء الأول يأخذ 5-10 دقائق
# مراقبة العملية:
docker compose -f docker-compose.sensor.yml logs -f

# اضغط Ctrl+C للخروج
```

#### الخطوة 2.9: التحقق من التشغيل

```bash
# انتظر دقيقة
sleep 60

# تحقق من حالة الحاويات
docker ps
```

**يجب أن ترى 4 حاويات:**
```
CONTAINER ID   IMAGE                    STATUS         NAMES
xxxx           minisiem-auth-collector  Up 2 minutes   minisiem-auth-collector
xxxx           minisiem-suricata        Up 2 minutes   minisiem-suricata
xxxx           minisiem-suricata-ship   Up 2 minutes   minisiem-suricata-shipper
xxxx           minisiem-flow-collector  Up 2 minutes   minisiem-flow-collector
```

#### الخطوة 2.10: التحقق من عمل Suricata

```bash
# تحقق من إنشاء ملف eve.json
ls -la data/suricata/
# يجب أن ترى: eve.json (قد يكون فارغاً في البداية)

# مراقبة سجلات Suricata
docker logs minisiem-suricata 2>&1 | tail -20
# ابحث عن: "All AFP capture threads running"
```

**✅ المرحلة 2 مكتملة!** - خادم الاستشعار يعمل ويرسل البيانات.

---

### 🧪 المرحلة 3: توليد حركة مرور واختبار الكشف

> **المدة المتوقعة:** 5-10 دقائق

#### الخطوة 3.1: إرسال أحداث تجريبية (من أي خادم)

```bash
cd ~/Analytical-Intelligence

# تثبيت Python requests إذا لم يكن موجوداً
pip3 install requests

# إرسال أحداث تجريبية مختلطة
python3 scripts/sample_sender.py \
  --url http://192.168.1.20:8000 \
  --api-key test-api-key-12345 \
  --count 20 \
  --type all
```

**الخرج المتوقع:**
```
Sending 20 events to http://192.168.1.20:8000
Event type: all
--------------------------------------------------
[1/20] AUTH:     Status=200
[1/20] SURICATA: Status=200
[1/20] FLOW:     Status=200
[2/20] AUTH:     Status=200
...
--------------------------------------------------
Done!
```

#### الخطوة 3.2: توليد حركة شبكة حقيقية (على خادم الاستشعار)

```bash
# على خادم الاستشعار - توليد حركة HTTP
curl -s http://example.com > /dev/null
curl -s http://google.com > /dev/null

# توليد DNS queries
nslookup google.com
nslookup facebook.com

# ping لعناوين مختلفة
ping -c 3 8.8.8.8
ping -c 3 1.1.1.1
```

#### الخطوة 3.3: محاكاة هجوم SSH (اختياري - للاختبار فقط)

```bash
# على خادم الاستشعار - محاكاة محاولات فاشلة
# هذا سيولد تنبيهات SSH
for i in {1..10}; do
  ssh -o ConnectTimeout=2 -o BatchMode=yes fakeuser@localhost 2>/dev/null
done

# التحقق من auth.log
sudo tail -5 /var/log/auth.log
```

#### الخطوة 3.4: تشغيل Nmap (للحصول على تنبيهات Suricata)

```bash
# على خادم الاستشعار - فحص المنافذ (سيولّد تنبيهات)
sudo apt install nmap -y

# فحص خفيف لخادم التحليل
nmap -sT -T2 192.168.1.20 -p 22,80,443,8000

# فحص الشبكة المحلية (سيولّد المزيد من التنبيهات)
nmap -sn 192.168.1.0/24
```

---

### 📊 المرحلة 4: رؤية النتائج في لوحة التحكم

> **المدة المتوقعة:** 5 دقائق

#### الخطوة 4.1: فتح لوحة التحكم

```
افتح في المتصفح: http://192.168.1.20:8000
```

#### الخطوة 4.2: ما ستراه في Dashboard

**قسم الإحصائيات (Stats Cards):**
```
┌─────────────┬───────────────┬────────────────┬──────────┐
│ Total Events│ Total         │ Detections     │ Devices  │
│ 150         │ Detections 45 │ (24h) 45       │ 1        │
└─────────────┴───────────────┴────────────────┴──────────┘
```

**قسم التنبيهات الأخيرة:**
```
┌──────────────────┬──────────┬────────────┬─────────────────────────┬───────────┐
│ Time             │ Severity │ Model      │ Label                   │ Device    │
├──────────────────┼──────────┼────────────┼─────────────────────────┼───────────┤
│ 2026-01-12 17:45 │ HIGH     │ suricata   │ ET SCAN Potential SSH   │ sensor-01 │
│ 2026-01-12 17:44 │ MEDIUM   │ ssh_lstm   │ SSH_BRUTE_FORCE         │ sensor-01 │
│ 2026-01-12 17:43 │ HIGH     │ network_ml │ PortScan                │ sensor-01 │
│ 2026-01-12 17:42 │ CRITICAL │ suricata   │ ET DOS Possible NTP DDoS│ sensor-01 │
└──────────────────┴──────────┴────────────┴─────────────────────────┴───────────┘
```

#### الخطوة 4.3: استكشاف صفحة التنبيهات

```
انتقل إلى: http://192.168.1.20:8000/alerts
```

**الفلاتر المتاحة:**
- **Severity:** CRITICAL, HIGH, MEDIUM, LOW
- **Model:** suricata, ssh_lstm, network_ml
- **Label Contains:** بحث نصي
- **Time Range:** آخر 5 دقائق، ساعة، 24 ساعة

**مثال تطبيق فلتر:**
```
Severity: HIGH
Model: suricata
→ Apply Filters
```

#### الخطوة 4.4: عرض تفاصيل تنبيه

1. انقر على زر **View** بجانب أي تنبيه
2. ستظهر نافذة بالتفاصيل الكاملة:

```json
{
  "signature_id": 2001219,
  "signature": "ET SCAN Potential SSH Scan",
  "category": "Attempted Information Leak",
  "src_ip": "192.168.1.10",
  "src_port": 45678,
  "dest_ip": "192.168.1.20",
  "dest_port": 22,
  "proto": "TCP"
}
```

#### الخطوة 4.5: استكشاف صفحات الأحداث

**أحداث SSH:**
```
http://192.168.1.20:8000/events/auth
```
- ترى سطور auth.log الخام
- كل سطر يحتوي على: timestamp, device, log line

**أحداث التدفق:**
```
http://192.168.1.20:8000/events/flows
```
- ترى تدفقات الشبكة
- المعلومات: src/dst IP:port, protocol, bytes, packets, duration

**أحداث Suricata:**
```
http://192.168.1.20:8000/events/suricata
```
- ترى جميع أحداث eve.json
- بما فيها التنبيهات والمعلومات

#### الخطوة 4.6: صفحة النماذج

```
http://192.168.1.20:8000/models
```

**ترى حالة النماذج:**
```
┌─────────────────────────────────────────────────┐
│ 🔐 SSH LSTM                          [LOADED]   │
│ Tokens: 12 | Window: 10 | Threshold: 0.5        │
├─────────────────────────────────────────────────┤
│ 🌐 Network ML                        [LOADED]   │
│ Features: 70 | Classes: 10                      │
│ Labels: BENIGN, Bot, DDoS, DoS...               │
├─────────────────────────────────────────────────┤
│ 🦈 Suricata IDS                      [ACTIVE]   │
│ Detection: Real-time | Latency: ~1 second       │
└─────────────────────────────────────────────────┘
```

---

### ✅ المرحلة 5: التحقق النهائي

#### قائمة التحقق:

- [ ] لوحة التحكم تعرض إحصائيات محدثة
- [ ] التنبيهات تظهر في قسم "Recent Alerts"
- [ ] الفلاتر تعمل في صفحة /alerts
- [ ] صفحات الأحداث تعرض البيانات
- [ ] صفحة /models تظهر النماذج كـ "LOADED"
- [ ] التحديث التلقائي كل 2 ثانية يعمل

#### اختبار التحديث التلقائي:

1. افتح Dashboard في المتصفح
2. من طرفية أخرى، أرسل أحداث:
   ```bash
   python3 scripts/sample_sender.py --count 5 --type suricata
   ```
3. راقب لوحة التحكم - ستتحدث الأرقام خلال 2-4 ثواني

#### التحقق من قاعدة البيانات:

```bash
# على خادم التحليل
docker exec -it minisiem-postgres psql -U siem -d minisiem -c "
SELECT 
  event_type, 
  COUNT(*) as count 
FROM raw_events 
GROUP BY event_type;
"

# المتوقع:
#  event_type | count
# ------------+-------
#  auth       |   50
#  suricata   |   30
#  flow       |   70
```

---

### 🎉 تهانينا!

**النظام يعمل بنجاح!** الآن لديك:

1. ✅ **خادم تحليل** يستقبل الأحداث ويحللها
2. ✅ **خادم استشعار** يجمع البيانات من الشبكة
3. ✅ **Suricata** يكشف الهجمات في الوقت الحقيقي
4. ✅ **نماذج ML** تصنف حركة الشبكة
5. ✅ **لوحة تحكم** تعرض التنبيهات والإحصائيات

---

## 🔧 استكشاف الأخطاء

### ❌ خطأ 401 Unauthorized

**السبب:** مفتاح API غير متطابق

**الحل:**
```bash
# تأكد أن X_API_KEY في الـ Sensor = INGEST_API_KEY في Analysis
# على Analysis:
cat .env | grep INGEST_API_KEY

# على Sensor:
cat .env | grep X_API_KEY
```

### ❌ واجهة الشبكة غير موجودة

**الخطأ:** `Interface ens33 not found`

**الحل:**
```bash
# اكتشف الواجهة الصحيحة
ip link show

# عدّل .env
nano .env
# غيّر NET_IFACE=ens33 إلى الواجهة الفعلية

# أعد التشغيل
docker compose -f docker-compose.sensor.yml down
docker compose -f docker-compose.sensor.yml up -d
```

### ❌ Suricata لا يكتب eve.json

**السبب:** صلاحيات أو إعدادات

**الحل:**
```bash
# تحقق من السجلات
docker logs minisiem-suricata

# تحقق من الملف
ls -la data/suricata/

# إذا كان فارغاً - أعد تشغيل
docker compose -f docker-compose.sensor.yml restart suricata
```

### ❌ صلاحيات قراءة /var/log/auth.log

**الخطأ:** `Permission denied: /var/log/auth.log`

**الحل:**
```bash
# على خادم الاستشعار
sudo chmod 644 /var/log/auth.log

# أو أضف المستخدم للمجموعة
sudo usermod -aG adm $USER
```

### ❌ مشاكل VMware/VirtualBox

**إذا كان الخادم افتراضياً:**
```bash
# تأكد من أن الشبكة Bridged وليس NAT
# في VMware: VM Settings > Network Adapter > Bridged

# تأكد من تفعيل Promiscuous Mode
# في VirtualBox: Settings > Network > Advanced > Promiscuous Mode: Allow All
```

---

## 📊 صفحات الواجهة

| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| لوحة التحكم | `/` | إحصائيات + آخر التنبيهات |
| التنبيهات | `/alerts` | قائمة التنبيهات مع فلاتر |
| أحداث SSH | `/events/auth` | سجلات auth.log |
| أحداث التدفق | `/events/flows` | تدفقات الشبكة |
| أحداث Suricata | `/events/suricata` | تنبيهات Suricata |
| النماذج | `/models` | حالة نماذج ML |

---

## ⏱️ تعريف "الوقت الحقيقي"

| المكون | زمن الاستجابة | التفسير |
|--------|---------------|----------|
| Suricata | ~1 ثانية | كشف فوري بالتوقيعات |
| SSH LSTM | 1-3 ثواني | تحليل فوري لكل سطر |
| Network ML | 2-8 ثواني | يعتمد على انتهاء التدفق |
| تحديث الواجهة | 2 ثانية | Polling interval |

---

## 🔐 ملاحظات أمنية

> [!WARNING]
> **تحذيرات هامة للإنتاج**

1. **غيّر API Key الافتراضي** قبل النشر:
   ```bash
   # إنشاء مفتاح عشوائي
   openssl rand -hex 32
   ```

2. **لا تكشف PostgreSQL** للشبكة الخارجية (الإعداد الحالي آمن)

3. **قيّد الوصول للـ Dashboard** عبر:
   - جدار الحماية (UFW)
   - أو إضافة مصادقة (مستقبلاً)

4. **للإنتاج الفعلي:**
   - استخدم HTTPS مع Nginx reverse proxy
   - أضف Rate limiting
   - راقب استخدام الموارد

---

## 📁 هيكل الملفات

```
Analytical-Intelligence/
├── .env.example                    # متغيرات البيئة
├── docker-compose.analysis.yml     # خادم التحليل
├── docker-compose.sensor.yml       # خادم الاستشعار
├── README.md                       # هذا الملف
│
├── scripts/
│   ├── init_db.sql                # مخطط قاعدة البيانات
│   ├── extract_models.sh          # استخراج النماذج (اختياري)
│   ├── sample_sender.py           # إرسال أحداث تجريبية
│   ├── analysis_up.sh             # تشغيل التحليل
│   └── sensor_up.sh               # تشغيل الاستشعار
│
├── models/
│   ├── ssh/                       # نموذج SSH LSTM
│   │   └── ssh_lstm.joblib
│   └── network/                   # نموذج ML للشبكة
│       ├── model.joblib
│       ├── feature_list.json
│       ├── label_map.json
│       └── preprocess_config.json
│
├── services/
│   └── backend/                   # خدمة FastAPI
│       ├── Dockerfile
│       ├── requirements.txt
│       └── app/
│           ├── main.py
│           ├── config.py
│           ├── db.py
│           ├── models_loader.py
│           ├── security.py
│           ├── schemas.py
│           ├── detectors/
│           ├── ingest/
│           ├── ui/
│           ├── templates/
│           └── static/
│
└── agents/
    ├── auth_collector/            # جامع سجلات SSH
    ├── suricata/                  # حاوية Suricata
    ├── suricata_shipper/          # شاحن تنبيهات Suricata
    └── flow_collector/            # جامع تدفقات الشبكة
```

---

## 📝 أوامر مفيدة

```bash
# === خادم التحليل ===
# تشغيل
docker compose -f docker-compose.analysis.yml up -d

# مراقبة السجلات
docker compose -f docker-compose.analysis.yml logs -f backend

# إيقاف
docker compose -f docker-compose.analysis.yml down

# إعادة بناء
docker compose -f docker-compose.analysis.yml up -d --build

# === خادم الاستشعار ===
# تشغيل
docker compose -f docker-compose.sensor.yml up -d

# مراقبة السجلات
docker compose -f docker-compose.sensor.yml logs -f

# إيقاف
docker compose -f docker-compose.sensor.yml down

# مراقبة Suricata
tail -f data/suricata/eve.json | jq '.'

# === قاعدة البيانات ===
# دخول PostgreSQL
docker exec -it minisiem-postgres psql -U siem -d minisiem

# نسخ احتياطي
docker exec minisiem-postgres pg_dump -U siem minisiem > backup.sql
```

---

## 🎯 أنواع الهجمات المكتشفة

### Suricata (التوقيعات)
- Port Scans
- SSH Brute Force
- SQL Injection
- XSS Attacks
- DoS/DDoS
- Malware Traffic
- Command Injection

### Network ML (10 فئات)
- BENIGN (حركة عادية)
- Bot
- DDoS
- DoS GoldenEye
- DoS Hulk
- DoS Slowhttptest
- DoS slowloris
- FTP-Patator
- PortScan
- SSH-Patator

### SSH LSTM
- SSH Brute Force
- SSH Anomaly (أنماط غير عادية)
- SSH Suspicious

---

## 📞 الدعم

للمساعدة:
1. راجع قسم استكشاف الأخطاء أعلاه
2. تحقق من سجلات Docker
3. تأكد من تطابق إعدادات الشبكة

---

**Mini-SIEM v1.0** - نظام SIEM مصغر للكشف عن التهديدات في الوقت الحقيقي 🛡️
